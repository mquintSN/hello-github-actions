name: Download file from azure, custom js
on: workflow_dispatch
jobs: 
  fetch_blob:
     name: Download file from azure and save to json file
     runs-on: ubuntu-latest
     steps:
       - uses: actions/checkout@v1
       - uses: actions/setup-node@v2
         with:
            node-version: '12'
       - run: npm install
       - uses: ./blob-fetch
         id: download_blob
         with:
           storage_account_name: sleepscience
           account_access_key: ${{ secrets.ACCOUNT_ACCESS_KEY }}
           container_name: states
           blob_prefix: us
           output_file: 'blob-fetch/results.json'
       - name: Commit file
         run: |	
          git config --local user.name  ${{ github.actor }}	
          git add blob-fetch/results.json	
          git commit -m "Updating the json file"	
       - name: Push changes 
         uses: ad-m/github-push-action@master	
         with:	
          github_token: ${{ secrets.TOKEN }}	
          force: true  

